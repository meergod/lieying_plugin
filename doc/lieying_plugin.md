:: lieying_plugin.md, language *Chinese* (`zh_cn`)
:: *last_update* `2015-08-14 22:52 GMT+0800 CST`

# 猎影 python 插件接口定义 version 0.3.0-test.7

author: `sceext <sceext@foxmail.com>`

此文件用来说明 猎影 `python` 插件接口 的定义. 


## 内容目录

+ **[0. 基本说明](#0-基本说明)**
  
  + **[0.1 猎影](#01-猎影)**
  + **[0.2 猎影 `python` 插件](#02-猎影-python-插件)**
  + **[0.3 猎影插件接口](#03-猎影插件接口)**
  + **[0.4 插件类型](#04-插件类型)**

+ **[1. 猎影插件的格式](#1-猎影插件的格式)**
  
  + **[1.1 编程语言 (`python3`)](#11-编程语言-python3)**
  + **[1.2 打包格式 (`zip`)](#12-打包格式-zip)**
  + **[1.3 入口文件 (`run.py`)](#13-入口文件-runpy)**
  + **[1.4 接口函数](#14-接口函数)**
  + **[1.5 文本格式 (*单行文本*, *多行文本*)](#15-文本格式)**
  + **[1.6 错误处理](#16-错误处理)**
  + **[1.7 插件升级](#17-插件升级)**
  + **[1.8 插件协作](#18-插件协作)**

+ **[2. 猎影插件接口函数定义](#2-猎影插件接口函数定义)**
  
  + **[2.1 `GetVersion()`](#21-getversion)**
  + **[2.2 `Config()`](#22-config)**
  + **[2.3 `Update()`](#23-update)**
  + **[2.4 `Parse()`](#24-parse)**
  + **[2.5 `ParseURL()`](#25-parseurl)**
  + **[2.6 `Search()`](#26-search)**

+ **[3. 示例](#3-示例)**


## 0. 基本说明

### 0.1 猎影

**猎影** 是一款 视频下载软件, 能够下载许多 在线视频网站 的视频. 
并且 界面 简洁美观, 方便易用. 

猎影 的 **官方网站** 是 <http://lieying.ilewo.cn/>

### 0.2 猎影 `python` 插件

**猎影插件** 是 *猎影* 的 *插件*, 是为了 **补充** 或 **增强** 猎影的功能. 

此文件中 所说的 *猎影插件*, 如果 没有 特殊 说明, 都指 *猎影 `python` 插件*. 

除了 `python` 接口 的 插件, *猎影* 也可能 支持 其它 接口 的 插件. 

### 0.3 猎影插件接口

**猎影插件接口** 就是 *猎影* 和 *猎影插件* 之间定义的接口. 

*猎影插件接口* 使用 **语义化版本**号 (semver 2.0.0, <http://semver.org/lang/zh-CN/>) 
来定义 接口 的版本, 以便处理 兼容性, 功能更新 等问题. 

### 0.4 插件类型

目前支持的 插件类型 有:

+ **解析插件** (*parse*)
  
  *解析插件* **解析** 出 *视频文件*的 **下载地址** *(URL)*. 
  
  + **输入** <br />
    **`URL`** 字符串
  
  + **输出** <br />
    *视频文件的下载地址 (URL)* 以及一些 *附加信息*. 
    
    *解析插件* 输出的 *下载地址 `URL`* 将被 *猎影* 用来 下载 相应文件. 
  
  比如, 输入 视频网站页面的 URL, 
  解析出 视频文件的下载地址, 视频标题 等信息. 

+ **搜索插件** (*search*)
  
  *搜索插件* 通过 **关键词** **搜索** 视频, 
  并获取 视频的 **URL**. 
  
  + **输入** <br />
    *关键词*, 等 字符串. 
  
  + **输出** <br />
    *视频的 URL* 以及一些 *附加信息*. 
    
    *搜索插件* 输出的 `URL` 应该能被 某些 *解析插件* 用作输入, 
    从而可以 获取 *视频文件 的 下载地址*. 
  
  比如, 输入 视频名称, 演员名字, 等, 搜索 有关 视频. 

+ *服务* **插件** (*plugin*)
  
  这种 插件, 不 直接 提供 功能. 而是 可以被 其它 插件 调用. 
  
  详见 [1.8 插件协作](#18-插件协作). 


## 1. 猎影插件的格式

### 1.1 编程语言 (`python3`)

*猎影插件* 使用 **[python3](https://docs.python.org/3/)** 作为 编程语言. 

*猎影* 会附带 **完整**的 `python 3.4` 运行环境, 包括 标准库, 和默认随 `python3` 安装的软件包. 
但是不附带其他使用 `pip` 安装的软件包. 

### 1.2 打包格式 (`zip`)

*猎影插件包* 是标准的 `zip` 格式的 **压缩包**. 
压缩包 中可包含任意数量的 文件, 其中只有一个 `run.py` 即 猎影插件的**入口文件**, 是**必须**的. 

### 1.3 入口文件 (`run.py`)

`run.py` 是 *猎影插件* 的 **入口文件**. 
`run.py` **必须**位于 压缩包 的**指定位置**. 

在 **指定位置** 只能有 1 个 `run.py` 文件. 

`run.py` 可用的 **指定位置** 有:

+ 压缩包 **根目录** 下 
+ 压缩包 根目录 下只有一个文件夹, 且 `run.py` 位于此 唯一 的文件夹下 

**比如**: 
`run.py` 有如下 2 种位置:

**1.** 假设 插件压缩包 根目录 下有 如下内容
```
 run.py
```

**2.** 假设 根目录 下只有一个 `plugin` 文件夹
```
 - plugin/
     run.py
```

### 1.4 接口函数

`run.py` 中定义有若干 **接口函数**. 

使用 插件 时, 猎影会先 **导入** `run.py` 这个 python 模块, 然后调用其中的函数. 

**举例说明**: 猎影导入 `run.py` 的动作 相当于 python 中的 `import run`, 
调用其中 `GetVersion()` 函数的动作相当于 python 中的 `run.GetVersion()` 

**导入冲突** <br />
*猎影* 会在 **同一个** `python` 运行环境 中 *导入* 多个 *插件* (的 `run.py` 模块). <br />
所以, 一个 *插件* 应该 避免 对 其它 *插件* 的 影响. 

目前定义的 *接口函数* 有:

+ `GetVersion()`
+ `Config()`
+ `Update()`

> 

+ `Parse()`
+ `ParseURL()`

> 

+ `Search()`

具体的 *函数定义*, 包括 *参数*, *返回值*, *功能说明*, 等, 请见[下文](#2-猎影插件接口函数定义). 

### 1.5 文本格式

在此 **明确定义** *猎影插件接口* 有关 *文本格式* 使用的 
2 个 概念, **单行文本** 和 **多行文本**. 

+ **编码** (`utf-8`)
  
  *猎影插件接口* 使用的 **文本** *(字符串)*, 全部使用 **`utf-8`** **编码**. 

+ **单行文本**
  
  **只有** 1 行 的文本. *文本 字符串* 中 **不允许 出现** `\r` 或 `\n` 字符. 
  
  **注**: 此处所说的 `\r` 和 `\n` 字符, 即 `python` 中定义的 *转义字符*. 
  如下所示: 
  
  ```
  >>> ord('\r')
  13
  >>> ord('\n')
  10
  >>> 
  ```

+ **多行文本**
  
  **可以** 有 多行 的文本. 
  注意, 是可以有多行, 在能够使用 *多行文本* 的位置, 
  都可以使用 *单行文本*. 
  
  *多行文本* 的 *文本 字符串* 使用 `\n` 表示换行. 
  
  也就是说, *多行文本 字符串* 中 **允许** 出现 `\n`, 但是 **不允许** 出现 `\r`. 

### 1.6 错误处理

当插件的 *接口函数* 执行中遇到错误, 请使用 `python` 的 `raise` 语句抛出一个错误. 

*猎影*会 **显示详细的错误信息**. 

比如

```
raise Exception('can not load page: http 404')
```

建议抛出错误时, 尽可能给出有用的详细错误信息, 以便帮助调试. 

**注意**: 抛出错误的 描述信息 *字符串* **允许**使用 **多行文本**, 

### 1.7 插件升级

+ *猎影插件* 具有 **升级** 功能, 具体 实现方式 请见 [2.3 `Update()`](#23-update). 
  
  *插件* 的 *升级* 功能 分为: 
  
  + **本地升级**
    
    通过 *导入* 更新版本 的 **本地** `zip` *插件包* 进行 *升级*. 
  
  + **网络升级**
    
    先从 **网络** 上 *下载* 更新版本 的 *插件包*, 然后进行 *本地升级*. 
    
    也就是说, *网络升级* 和 *本地升级* 的区别, 就是 从网上下载 `zip` *插件包*. 
    剩下的过程, 和 *本地升级* 一样. 

+ **本地升级** 说明
  
  **导入** `zip` *插件包* 时, *猎影* 会这么做: 
  
  1. **解压** `zip` *压缩包* 至 一个 *临时目录*, 假设为 `tmp_a`. 
     `run.py` 就位于 `tmp_a` 目录下. 
  
  2. **导入** `run.py` 并且 **调用** 其中的 `GetVersion()` 函数, 获取 *插件*的 相关信息. 
     
     并且进行 必要的 **检查**, 比如 检查 *猎影插件接口 版本* (`port_version`), 等. 
  
  3. 检查 插件的 **`uuid`**. 
     
     如果 没有 相同的 插件, 就将 `tmp_a` 中的 全部文件, 
     **移动** 到 以 插件的 `uuid` 命名 的 目录中, 导入 [**完成**]. 
  
  4. 如果 发现 相同的 插件, 会 *导入* **原有**插件 的 `run.py`, 
     检查 是否 定义 `Update()` 函数. 
     
     如果 **没有定义**, 就说明 *原插件* 不支持 升级功能, 将进行 简单的 *文件替换*. 
     导入 [**完成**]. 
     
     **文件替换**: 
     就是 **删除** 以 `uuid` 命名 的 目录 中的 *全部文件*, 
     然后 把 `tmp_a` 中的 全部 文件, 移动到 `uuid` 命名 的 目录中. 
  
  5. 如果 **定义了** `Update()` 函数, 将 **通知** **原有**插件 进行升级, 
     即 调用 *原有插件* 的 `Update()` 函数, 其 `local_path` 参数, 
     传入 `tmp_a` 目录 的 *绝对路径*. 
     
     然后 *插件* 自行进行 *升级处理*. 
  
  6. *原有 插件* 的 `Update()` 函数 返回, 表示 *升级处理* 完成. 
     
     猎影 会 删除 `tmp_a` 目录, 及其中的 *全部文件*. 
     导入 [**完成**]. 

+ **连续升级** 特性
  
  当 需要对一个 *插件* 进行 *升级* 操作时, 
  *猎影* 会 调用 `Update()` 函数. 
  
  如果 `Update()` 返回的 不是 `''` *(空字符串)*, 
  说明 需要进行更多处理. <br />
  这时, *猎影* 会很快 再次调用 `Update()` 函数. 
  
  *猎影* 会 重复调用 `Update()` 函数, 直到 `Update()` 返回 `''`, 
  或者 达到某个 次数限制 (这时会提示 升级错误). 
  
  *插件* 可以使用 **连续升级** 特性 进行一些 复杂的 升级操作. 

+ **首次导入 立即升级** 特性
  
  当 **首次 导入** *(安装)* 某个 *插件* (的 `zip` 压缩包) 时, 
  *猎影* 会在 *插件* 导入后 立即 尝试 *升级* 操作. 
  即 调用 `Update()` 函数. 
  
  *首次导入* 是指 这个*插件* (使用 `uuid` 标记) 之前不存在. 
  
  **注意**: *猎影* 可能会在 *设置* 中 关闭 此特性. 
  *插件* 不能预期 此特性 始终生效. 

### 1.8 插件协作

*猎影* 的 *插件* 之间, 可以 进行 简单的 协作. 

+ **插件根目录** (`uuid` 命名)
  
  *插件根目录* 是指 `run.py` 所在的 目录. 
  
  + 此目录将根据 *插件*的 `uuid` (`GetVersion()` 返回的) 命名. 
    
    比如: 
    `uuid` 为 `5fbafa0b-cbb1-4bba-b510-d34cddc37e14` 的 *插件* 
    其 *根目录* 为 `_5fbafa0b_cbb1_4bba_b510_d34cddc37e14`. 
    
    **转换方式** <br />
    将 `uuid` 中的 全部 `-` 替换成 `_`, 
    然后在最前面加上一个 `_` 字符, 就是 *插件根目录* 的名称. 
  
  + *插件根目录* 的**位置**
    
    **所有**的 *猎影插件* 的 *根目录* 都位于 **同一个** 目录 中, 
    即 *猎影* 的 *插件目录*. 

+ **导入 其它插件**
  
  *插件* 可以使用 以下类似方法, 导入 *其它插件*. 
  
  ```
  from _5fbafa0b_cbb1_4bba_b510_d34cddc37e14 import run as plugin1
  ```


## 2. 猎影插件接口函数定义

### 2.1 `GetVersion()`

([`GetVersion()` 示例](#3-示例))

+ **函数定义**
  
  ```
  def GetVersion()
  ```
  **注意**: **所有类型**的插件都**必须**定义此接口函数. 

+ **功能说明** <br />
  用于返回插件的版本号等有关信息. 
  具体返回的信息及其格式, 请见 *返回值* 说明. 

+ **参数**
  
  **无** <br />
  此函数没有定义参数. 

+ **返回值**
  
  类型: **`json` 字符串**
  
  json 信息结构: 
  
  ```
  {
      "port_version" : "0.3.0", 
      "type" : "", 
      "uuid" : "", 
      "version" : "", 
      "name" : "", 
      
      "filter" : [], 
      
      "author" : "", 
      "copyright" : "", 
      "license" : "", 
      "home" : "", 
      "note" : ""
  }
  ```
  
  以下项目, **所有类型**的插件都**必须**定义: 
  
  + **`port_version`** 类型: 字符串 *(单行文本)* <br />
    插件所使用的 *猎影插件接口* 的 **版本号**. 
    
    [当前](#03-猎影插件接口) 版本号是 `0.3.0`
  
  + **`type`** 类型: 字符串 *(单行文本)* <br />
    插件类型. 
    
    目前支持的 [插件类型](#04-插件类型) 有
    
    + **`parse`**: 解析插件
    + **`search`**: 搜索插件
    + **`plugin`**: *服务* 插件
  
  + **`uuid`** 类型: 字符串 *(单行文本)* <br />
    插件的 `uuid` 标识. 
    
    猎影 将**仅根据** *uuid* 来识别一个插件, 而不用 *插件名称* 等其它信息. 
    就是说, 只要 uuid 相同, 猎影就会认为这是同一个插件. 
    
    开始写一个 *猎影插件* 时, 应该使用 uuid 生成软件 (比如 `uuidgen`) 为此 插件 生成一个 `uuid`. 
    
    在发布插件的新版本时, **除非故意**这么做, 否则**不要修改**此插件的 *uuid*. 
    
    *猎影* 会提示插件升级, 并且 通知 *原有插件* 进行 *升级操作*, 或者 使用 新版插件 *替换* 旧版插件. 
  
  + **`version`** 类型: 字符串 *(单行文本)* <br />
    插件 版本号. 
    
    这个是 插件 自己的 版本号. 
    使用何种版本号规范, 由插件自己决定. 
    *猎影插件接口* 不做统一规定. 
  
  + **`name`** 类型: 字符串 *(单行文本)* <br />
    插件名称. 
    
    这个 *插件名称* 将显示在 *猎影* 的 *插件列表* 中. 
    
    **注意**: 猎影 不使用 *插件名称* 来唯一识别一个插件, 而是使用 `uuid`. 
  
  
  以下项目, 类型为 **`parse`** *(解析插件)* 的插件, **需要**定义. 
  
  + **`filter`** 类型: 数组 (`list`, `[]`, `Array`) <br />
    返回 `Parse()` 函数 支持的 文本输入的 *正则表达式* 数组. 
    数组内容为 字符串, 每个字符串 是一个 正则表达式. 
    
    在调用插件的 `Parse()` 函数之前, 猎影会 尝试 使用 `filter` 数组中的 
    *正则表达式* 匹配 *输入的字符串*. 如果匹配, 将会调用 此插件, 否则不会. 
  
  
  以下项目, **所有类型** 的插件都 **可以** 定义, 也就是说, 是 *可选*的项目, 不是必须定义. 
  
  + **`author`** 类型: 字符串 *(多行文本)* <br />
    插件 的 作者信息. 
  
  + **`copyright`** 类型: 字符串 *(多行文本)* <br />
    插件 的 版权信息. 
  
  + **`license`** 类型: 字符串 *(多行文本)* <br />
    插件 使用的 **许可证** (*license*). 
    
    比如 `GNU GPLv3+`, `MIT`, `unlicense`, 等. 
  
  + **`home`** 类型: 字符串 *(多行文本)* <br />
    插件 的 首页地址. 
    
    可以是 插件的 官方网站, 插件的 github 项目主页, 等. 
  
  + **`note`** 类型: 字符串 *(多行文本)* <br />
    插件 的 描述信息. 

+ **更多说明**
  
  + 插件可以在 `GetVersion()` 的返回的结果中 自己添加 一些其它的信息. 
    然而, *猎影* 并不保证会使用这些信息, *猎影* 可能直接忽略它们. 
    
    但是, 已经被 *猎影插件接口* 定义的项目, **必须**按照 *猎影插件接口* 定义的方式使用. 

### 2.2 `Config()`

(`Config()` 没有示例)

+ **函数定义**
  
  ```
  def Config(show_window=False)
  ```
  **注意**: **所有类型**的插件都**可以**定义此接口函数. 

+ **功能说明** <br />
  用于 插件 的 配置. 
  
  猎影插件管理界面, 会提供 *配置插件* 按钮. 
  用户点击此按钮之后, 猎影会调用此函数, 通知插件用户想要启动配置程序. 
  
  剩下的全部事情, 由插件自己处理. 

+ **参数**
  
  + **`show_window`** 类型: `bool` (`boolean`) <br />
    是否显示 配置界面 (插件 自己实现的 图形配置界面). 
    
    为 `True` 时表示插件可以启动 配置界面, 
    为 `False` 时表示插件 **不能** 启动 配置界面. 
    
    *猎影* 会 2次 调用 `Config()`. 
    更多信息 请见 下方. 

+ **返回值**
  
  **无** <br />
  此函数没有定义返回值. 

+ **更多说明**
  
  + 如果插件**不支持配置**, 或者没有自带的配置程序, 请**不要定义** `StartConfig()` 函数. 
    
    这样猎影的*插件管理*界面, 就不会再显示 *配置插件* 按钮. 
  
  + 建议使用 python3 自带的 [`tkinter`](https://docs.python.org/3/library/tk.html) 
    组件提供图形界面的配置程序. 
  
  + *插件* 可以自行使用 *配置文件* 保存 配置信息. 配置文件 可以与插件的 python 代码放在一起. 
    
    *猎影* 不会处理 插件 配置信息的 保存, *插件* 需要 自行处理. 
  
  + *猎影插件* 运行在一个 **后台** 进程 中. *(windows 平台)* <br />
    *后台进程* **无法显示** 窗口 *(图形界面)*. 
    
    所以, *猎影* 需要在 另外一个 *前台* 进程中调用 *插件* 的 `Config()` 函数, 
    让 *插件* 有机会 显示 自己实现的 图形窗口 配置界面. 
    
    + *猎影* 会在 另一个进程中 `import` 插件, 然后调用 `Config()` 
      并且 `show_window` 参数为 `True`. 
      
      此时插件 应该 显示 图形配置界面. 
    
    + 第 1 次 调用的 `Config()` 返回后, *猎影* 会在 *后台进程* 中, 
      第 2 次 调用 `Config()` 并且 `show_window` 参数为 `False`. 
      
      此时插件 应该 重新加载 配置, 并且 应用新的配置. 

### 2.3 `Update()`

(`Update()` 没有示例)

+ **函数定义**
  
  ```
  def Update(local_path='')
  ```
  **注意**: **所有类型**的插件都**可以**定义此接口函数. 

+ **功能说明** <br />
  用于 *插件* 的 **升级** 功能. 
  
  + **本地升级**
  + **网络升级**
  
  *插件* *升级* 功能 的 更多说明, 请见 [1.7 插件升级](#17-插件升级). 

+ **参数**
  
  + **`local_path`** 类型: 字符串 *(单行文本)* <br />
    本地升级 路径. 
    
    + **空** <br />
      此参数 为 `''` *(空字符串)*. 
      
      表示 希望 *插件* 进行 *网络升级*. 
      
      插件 应通过 网络 检查 更新版本, 并且进行升级. 
      如果 **没有 更新**版本, 就**不进行** 升级操作. 
      
      *猎影* 可能会 定时 调用 `Update()` 以便实现 插件的 *自动升级*. 
    
    + **绝对路径** <br />
      此参数 为 某个 *绝对路径*, 此 路径 指向 更新的 *插件根目录*. 
      
      表示 希望 *插件* 进行 *本地升级*. 
      
      *本地升级* 的 更多说明, 请见 [1.7 插件升级](#17-插件升级). 

+ **返回值**
  
  类型: 字符串 *(单行文本)* <br />
  进行 *网络升级* 时, 指向 新的 `zip` *插件包*. 
  
  + **空** <br />
    *返回值* 为 `''` *(空字符串)*. 
    
    表示 不需要 更多 操作. 升级 **到此 结束**. 
  
  + **网络地址** (`http`, `https`) <br />
    *返回值* 为 一个 `URL`. 
    
    `URL` 支持 以下 协议: 
    
    + **`http`**
    + **`https`**
    
    表示 需要 *猎影* 从 此 *URL* 下载 `zip` *插件包*, 然后 进行 *本地升级*. 
  
  + **本地路径** <br />
    *返回值* 为 一个 *本地 路径*, 指向 一个 `zip` *插件包*. 
    
    表示 需要 *猎影* 从 此 *插件包* 进行 *本地升级*. 
    
    *本地路径* 支持 以下 格式: 
    
    + **绝对路径** <br />
      使用 *绝对路径* 来 指向 本地 *插件包*. 
    
    + **相对路径** <br />
      此 *相对路径* 相对于 **插件根目录**. 
    
    + **`URL`** (`file://`) <br />
      和 *绝对路径* 类似, 不过是一个 `file://` 开头的 `URL`. 

+ **更多说明**
  
  + 如果 *插件* **不支持** 此处 定义的 *升级* 功能, 请 **不要定义** `Update()` 函数. 
    
    *猎影* 将会处理 *插件*的 升级 (替换), 就是 *直接删除* *旧*插件 的 *全部*文件, 
    然后 导入 *新*插件包 的 *全部*文件. 
    
    如果 支持 *升级* 功能, *插件* 需要进行 具体的 *升级处理*, 比如 *文件替换* 等. 
    *猎影* 只是提供 简单的 支持接口, 更多事情, 还需要 插件 自己来做. 
  
  + **插件根目录** 指的是 `run.py` 所在的目录. 而不管 `zip` 插件包 的 打包方式 如何. 
  
  + *插件* 可以使用 `python` 的 `zipfile` <https://docs.python.org/3/library/zipfile.html> 
    模块, **读写** `zip` 文件. 
  
  + **升级 过程中 输出信息 (文本)**
    
    在 `Update()` 函数 执行过程中, 
    *插件* 可以使用 `python` 的 `print()` 函数 
    输出 一些信息 (文本). 
    
    *猎影* 会显示 这些 文本. 
    
    *插件* 可以 这样 让用户 看到 升级的过程. 
    也可以 方便 *调试* 和 *除错*. 
  
  + **升级 错误**
    
    *插件* 的 升级过程, 比较 复杂, 容易 发生各种 错误. 
    
    在 执行 `Update()` 函数 过程中, 
    如果 发生错误, 请按照 [1.6 错误处理](#16-错误处理) 定义 的方式, 
    用 `raise` 抛出 错误 及 相关 错误信息. 
    
    *猎影* 会进行 适当的 处理. 
  
  + *插件* **升级** *(替换)* 之后, *猎影* 会 
    调用 `GetVersion()` 获取 并 更新 *插件*的 有关信息, 
    并且 重新 *导入* 新的 插件 (导入 *新插件* 的 `run.py`). 

### 2.4 `Parse()`

([`Parse()` 示例](#3-示例))

+ **函数定义**
  
  ```
  def Parse(url)
  ```
  **注意**: 以下类型的插件需要定义此接口函数:
  + `parse` (解析插件)

+ **功能说明** <br />
  解析 *用户输入的字符串*, 返回解析结果. 
  
  用户输入的文本, 是一个 URL, 但可能是 `http`, `https`, `ftp`, 等 不同的*协议*. 
  这取决于 `GetVersion()` 返回信息中 `filter` *正则表达式* 的定义. 
  
  根据不同的 用户输入, 插件可自行选择 *解析模式*. 
  返回 *若干*个 解析*结果*. 
  
  目前支持的 **解析模式** 有:
  
  + **单视频解析** <br />
    用户输入的 URL, 应该解析出**一个**视频. <br />
    此时, 返回 这个视频的若干可选的 不同格式, 供用户选择. 
    
    比如, 用户输入 某一集 电视剧 播放网页的 URL, 
    此时可以解析出 1080p, 720p 等不同格式的视频. 
  
  + **多视频解析** <br />
    用户输入的 URL, 应该解析出**多个**视频. <br />
    此时, 返回 若干 视频项目, 供用户选择. 
    
    比如, 用户输入 某一部 电视剧 剧集页面的 URL, 
    此时可以解析出 每一集 电视剧 播放页面 的 URL. 

+ **参数**
  
  + **`url`** 类型: 字符串 *(单行文本)* <br />
    用户输入的字符串. 

+ **返回值**
  
  类型: **`json` 字符串**
  
  `Parse()` 返回的 *json 信息结构* 整体上是一个 数组 (`list`, `[]`, `Array`). 
  数组 的 项目 是 `dict` (`{}`, `Object`). 
  
  每个 项目 是一个 解析结果, 由 插件 决定 选用 何种类型. 
  
  *单视频解析*模式, 和 *多视频解析*模式, 返回结果是不一样的. 
  
  **单视频解析**
  
  json 信息结构: 
  
  ```
  [
      {
          "type" : "formats", 
          "name" : "", 
          "data" : [
              {
                  "label" : "", 
                  "ext" : "", 
                  "size" : ""
              }
          ]
      }
  ]
  ```
  
  + **`type`** 类型: 字符串 *(单行文本)* <br />
    返回结果的类型, 值 **必须** 为 `formats`, 表示这是 *单视频解析* 的结果. 
  
  + **`name`** 类型: 字符串 *(单行文本)* <br />
    视频的标题. 
    
    *视频标题* 的具体文本格式, 由插件自行处理, *猎影插件接口* 不做统一规定. 
  
  + **`data`** 类型: 数组 (`list`, `[]`, `Array`) <br />
    *视频格式* 信息. 
    
    数组内容为若干 `dict` (`{}`, `Object`). 
    每个 `dict` 表示一种视频的格式. 
  
  + **`label`** 类型: 字符串 *(单行文本)* <br />
    格式说明字符串. 用来表示并区分不同的视频格式. 
    
    猎影会在列表中显示 此字符串, 供用户选择. 
    
    `label` *字符串* 的格式由插件自行处理, *猎影插件接口* 不做统一规定. 
  
  + **`ext`** 类型: 字符串 *(单行文本)* <br />
    视频文件的扩展名. 用来表示这种格式视频的 视频文件格式. 
    
    比如 `mp4`, `flv`, 等. 
  
  + **`size`** 类型: 字符串 *(单行文本)* <br />
    此种格式视频的文件大小 (如果有多个视频文件, 指它们的总大小). 
    
    以方便用户阅读的格式提供, 比如 `1.01 GB`. 
  
  
  **多视频解析**
  
  json 信息结构:
  
  ```
  [
      {
          "type" : "list", 
          "title" : "", 
          "data" : [
              {
                  "no" : "", 
                  "subtitle" : "", 
                  "name" : "", 
                  "url" : ""
              }
          ]
      }
  ]
  ```
  
  + **`type`** 类型: 字符串 *(单行文本)* <br />
    返回结果的类型, 值 **必须** 为 `list`, 表示这是 *多视频解析* 的结果. 
  
  + **`title`** 类型: 字符串 *(单行文本)* <br />
    这些视频的 整体标题. 
    
    比如, 解析结果是 某部电视剧的 每一集, 那么 此标题, 可选为 这部电视剧的 名称, 不包含 第几集. 
    
    如果 不便给出此标题, 可以返回 `""` *(空字符串)*. 
    
    `title` *字符串* 的格式由插件自行处理, *猎影插件接口* 不做统一规定. 
  
  + **`data`** 类型: 数组 (`list`, `[]`, `Array`) <br />
    *视频项目* 信息. 
    
    数组内容为若干 `dict` (`{}`, `Object`). 
    每个 `dict` 表示一个视频项目. 
  
  + **`no`** 类型: 字符串 *(单行文本)* <br />
    能简短的 将此视频 区别出来 的字符串. 
    
    比如, 解析结果是 某部电视剧的每一集, 此处可以使用 *第x集* 这种形式. 
    
    `no` *字符串* 的具体格式, 由插件自行处理, *猎影插件接口* 不做统一规定. 
  
  + **`subtitle`** 类型: 字符串 *(单行文本)* <br />
    此视频的 **小标题**. 
    
    比如, 有些视频网站, 给电视剧的每一集, 都加一个 *小标题*. 
    
    如果 不便给出, 可以返回 `""` *(空字符串)*. 
    
    `subtitle` *字符串* 的格式由插件自行处理, *猎影插件接口* 不做统一规定. 
  
  + **`name`** 类型: 字符串 *(单行文本)* <br />
    此视频的 **完整标题**. 
    
    此处的 `name` 应该和 *单视频解析* 时, 解析这个视频返回的 `name` *字符串* 保持一致. 
  
  + **`url`** 类型: 字符串 *(单行文本)* <br />
    此视频的 URL. 
    
    当进行 *单视频解析* 时, 此字符串会被 **按原样** 传入 `Parse()` 的 `url` 参数. 

+ **更多说明**
  
  + 当返回 *多视频解析* 结果后, 猎影 很可能会再次调用 `Parse()` 进行 *单视频解析*, 
    且传入的 `url` 就是 *多视频解析* 返回的 `url`. 
  
  + 所以, *多视频解析* 返回的 `url` *字符串*, 不一定必须是 URL. 也可以是其它格式. 
    只要插件 在进一步的 *单视频解析* 中能够识别处理 即可. 
  
  + 插件 可以 同时 返回 多个 *单视频解析*结果 以及 *多视频解析*结果. 

### 2.5 `ParseURL()`

([`ParseURL()` 示例](#3-示例))

+ **函数定义**
  
  ```
  def ParseURL(url, label, i_min=None, i_max=None)
  ```
  **注意**: 以下类型的插件需要定义此接口函数:
  + `parse` (解析插件)

+ **功能说明** <br />
  解析 视频文件的 **下载地址**. 
  
  当调用 `Parse()` 进行 *单视频解析* 并且获取 视频格式 后, 
  猎影 会调用 `ParseURL()` 解析视频文件的下载地址, 然后进行下载. 

+ **参数**
  
  + **`url`** 类型: 字符串 *(单行文本)* <br />
    此字符串与调用 `Parse()` 进行 *单视频解析* 时传入的 `url` 相同. 
  
  + **`label`** 类型: 字符串 *(单行文本)* <br />
    此字符串与调用 `Parse()` 进行 *单视频解析* 时返回结果中的一个 `label` *字符串* 相同. 
    表示需要解析此种格式视频的文件下载地址. 
  
  + **`i_min`** 类型: 整数 或 `None` <br />
    **希望获取**的 分段文件信息的 **最小 序号**, 按数组下标, 0 表示第 1 段. 
    
    若为 `None` 表示不加限制. 
  
  + **`i_max`** 类型: 整数 或 `None` <br />
    **希望获取**的 分段文件信息的 **最大 序号**, 按数组下标, 0 表示第 1 段. 
    
    若为 `None` 表示不加限制. 

+ **返回值**
  
  类型: **`json` 字符串**
  
  json 信息结构: 
  
  ```
  [
      {
          "protocol" : "http", 
          "args" : {}, 
          "value" : ""
      }
  ]
  ```
  
  返回的 json 结果, 整体是一个 数组 (`list`, `[]`, `Array`), 
  数组的每一项是一个 `dict` (`{}`, `Object`). 
  
  每个 `dict` 表示一个 **分段视频文件** 的*下载信息*. 
  
  之所以这样设计, 是因为 有许多视频, 都是**分段视频**, 需要下载所有 *分段视频文件*, 
  并且 *合并* 之后, 才能得到完整的视频. 
  
  + **`protocol`** 类型: 字符串 *(单行文本)* <br />
    下载使用的 *协议*. 
    
    目前支持以下协议: 
    
    + **`http`**
  
  + **`args`** 类型: `dict` (`{}`, `Object`) <br />
    下载协议所需的额外信息. 
    
    + 对于 `http` 协议, `args` 是额外的 **http 头**. 
      
      比如 `User-Agent`, `Referer`, 等. 
      
      如果不需要定义 额外的 *http 头*, 应该返回 `{}` *(空 `dict`)*. 
  
  + **`value`** 类型: 字符串 *(单行文本)* <br />
    下载地址. 
    
    + 对于 `http` 协议, 是类似于 `http://` 这样的字符串. 

+ **更多说明**
  
  `ParseURL()` 以 数组 格式返回下载信息, 对于 分段视频, 
  **插件一定要保证返回的分段文件的顺序正确 !!!**
  
  比如, 一个 10 分钟的视频, 被分成 2 段, 第 1 段是 0 至 6 分钟, 第 2 段是 7 至 10 分钟, 
  那么 插件返回的下载信息数组, 一定要保证 第 1 段 在 第 2 段 的前面. 
  
  猎影会 按照插件返回的 分段文件顺序 合并视频. 
  如果插件返回的顺序不正确, 将会导致 合并后的视频 **播放时间错乱 !!!**

+ **对 `i_min` `i_max` 参数的说明**
  
  `i_min` 和 `i_max` 参数, 用于 *猎影插件* 的一个 **高级特性**: **逐个解析**. 
  
  简单来说, 就是猎影每调用一次 `ParseURL()` 将 
  只 解析并返回 1 个 或 仅仅几个 分段文件的下载信息, 
  而不是返回全部. 
  
  只有在 **解析较慢** 的情况下, 此特性才有明显的效果. 
  
  **多数情况**下, 不需要使用此特性, 可以**直接忽略** `i_min` 和 `i_max` 参数. 
  
  并且, 即使插件 **忽略** `i_min` 和 `i_max` 参数, 也**不会造成任何错误**, 
  *猎影* 以及 *猎影插件* 仍然能够正常工作. 
  
  不过, 如果支持此特性, 在某些情况下, 能够获得更好的性能. 
  
  由于 `i_min` 和 `i_max` 参数 稍显 复杂, 在此不再 详细说明. 
  如有需要, 请阅读 
  [lieying_plugin_ParseURL.md](https://github.com/sceext2/lieying_plugin/blob/plugin-you-get/doc/lieying_plugin_ParseURL.md)
  获取更多详细信息. 

### 2.6 `Search()`

(`Search()` 暂无 示例)

+ **函数定义**
  
  ```
  def Search(input_text, start=0, page_size=-1, sort=None, reverse=False)
  ```
  **注意**: 以下类型的插件需要定义此接口函数:
  + `search` (搜索插件)

+ **功能说明** <br />
  根据 *用户输入的字符串* 搜索 相关视频, 返回 相关信息. 

+ **参数**
  
  + **`input_text`** 类型: 字符串 *(单行文本)* <br />
    用户输入的 文本字符串. 
  
  + **`start`** 类型: 整数 <br />
    输出结果 开始条目 的 序号, 按照 数组 下标, `0` 表示 第 1 个 条目. 
    
    此参数用于 搜索结果 的 分页显示. 
  
  + **`page_size`** 类型: 整数 <br />
    输出结果 中 **最多** 应该包含的 条目 个数. 
    `-1` 表示 没有限制. 
    
    此参数用于 搜索结果 的 分页显示. 
  
  + **`sort`** 类型: 字符串 *(单行文本)* 或 `None` <br />
    指定 输出结果 的 排序方式. 
    `None` 表示 不特别指定排序方式, 使用 插件自己 默认 的排序方式. 
    
    可能的 排序方式 有:
    
    + **`match`** 最佳匹配
    
    + **`new`** 最新
    
    + **`hot`** 最热门
    
    **注意**: 如果 插件 不支持 排序, 或者 不支持 指定的 排序方式, 
    **可以 忽略** 此参数. 
  
  + **`reverse`** 类型: `bool` (`boolean`) <br />
    指定 *升序* 或者 *降序* 排序. 
    此参数与 `python` 中的 `reverse` 参数一致. 
    
    + 为 `False` 时表示 **升序**. 比如 `[1, 2, 3]` 
    + 为 `True` 时表示 **降序**. 比如 `[3, 2, 1]` 
    
    **注意**: 如果 插件 不支持 指定的 排序方式, 
    **可以 忽略** 此参数. 

+ **返回值**
  
  类型: **`json` 字符串**
  
  json 信息结构: 
  
  ```
  {
      "type" : "search", 
      
      "total" : -1, 
      "more" : false, 
      
      "start" : 0, 
      "sort" : null, 
      "reverse" : null, 
      
      "data" : [
          {
              "url" : "", 
              "title" : "", 
              "sum" : 0, 
              
              "img" : "", 
              "note" : "", 
              
              "actors" : "", 
              "area" : "", 
              "class" : "", 
              "last_update" : "", 
              "quality" : "", 
              "site" : "", 
              "size_byte" : 0, 
              "size_px" : [
                  -1, 
                  -1
              ], 
              "subtitle" : "", 
              "time_s" : 0, 
              "year" : ""
          }
      ]
  }
  ```
  
  + **`type`** 类型: 字符串 *(单行文本)* <br />
    返回结果的类型, 值 **必须** 为 `search`, 
    表示这是 普通列表形式 的搜索结果. 
    
    以后 可能会支持 其它类型的 搜索结果. 
  
  + **`total`** 类型: 整数 <br />
    搜索结果 的 条目总数. 
    
    如果 不知道, 应该返回 `-1`. 
  
  + **`more`** 类型: `bool` (`boolean`) <br />
    能否返回 更多结果. 
    
    如果没有更多结果, 应返回 `false`, 否则应返回 `true`. 
  
  + **`start`** 类型: 整数 <br />
    返回结果 开始条目 的 序号, 按照 数组 下标, `0` 表示 第 1 个 条目. 
    
    此项目与 参数 中的 `start` 含义一致. 
    用于 搜索结果 的 分页显示. 
  
  + **`sort`** 类型: 字符串 *(单行文本)* 或 `null` <br />
    结果使用的 排序方式. 
    `null` 表示 没有使用 指定的 排序方式, 
    或者使用 插件自己的 默认 排序方式. 
    
    其它可能的排序方式, 请见 *参数* 中的 `sort` 说明. 
  
  + **`reverse`** 类型: `bool` (`boolean`) 或 `null` <br />
    *升序* 或 *降序* 排序. 
    
    `false` 表示 *升序*, `true` 表示 *降序*. 
    
    `null` 表示 既不是 *升序* 也不是 *降序*. 
  
  + **`data`** 类型: 数组 (`list`, `[]`, `Array`) <br />
    *视频条目* 信息. 
    
    数组内容为若干 `dict` (`{}`, `Object`). 
    每个 `dict` 表示一个视频条目. 
    
    如果 没有返回 内容, 比如 没有搜索结果, 或者 分页显示参数 (`start`) 超出范围, 
    应该返回 `[]` *(空 数组)*. 
  
  以下项目, 每个 *视频条目* 都 **必须**定义: 
  
  + **`url`** 类型: 字符串 *(单行文本)* <br />
    此 视频条目 对应的 `URL`. 
    
    此 URL 应该能够被 某些 *解析插件* 用作输入, 并且 解析出 视频文件的下载地址. 
  
  + **`title`** 类型: 字符串 *(单行文本)* <br />
    视频标题. 
    
    具体文本格式, 由插件自行处理, *猎影插件接口* 不做统一规定. 
  
  + **`sum`** 类型: 整数 <br />
    此 *视频条目* 对应的 *视频个数*. 
    
    如果 只有 1 个视频, 应返回 `1`. 
    
    如果有多个视频, 比如 是一个 *剧集*, 应返回 剧集中视频的个数, 比如 `40`. 
    
    如果不知道 剧集 中的视频个数, 应返回 `-1`. 
  
  以下项目, 每个 *视频条目* 都 **可以** 定义. (可选的) 
  但是, 如果 不能 返回 适当的 对应数据, 比如 不知道 或 无法获取 某种 信息, 
  返回的 *视频条目* 信息中, 请 **不要 包含** 此项目. 
  
  + **`img`** 类型: 字符串 *(单行文本)* <br />
    视频 缩略图 的 `URL`. 
    
    此图片会显示在 *猎影* 搜索结果列表 中. 
  
  + **`note`** 类型: 字符串 *(多行文本)* <br />
    视频描述 信息, 比如 剧情简介 等. 
  
  + **`actors`** 类型: 字符串 *(单行文本)* <br />
    视频的 演员 信息. 比如 主演名字 等. 
    
    具体文本格式, 由插件自行处理, *猎影插件接口* 不做统一规定. 
  
  + **`area`** 类型: 字符串 *(单行文本)* <br />
    视频的 地区 信息. 比如 *美国*, *韩国* 等. 
    
    具体文本格式, 由插件自行处理, *猎影插件接口* 不做统一规定. 
  
  + **`class`** 类型: 字符串 *(单行文本)* <br />
    视频类型. 
    比如 *古装剧*, *电影*, *于正剧*, 等. 
    
    具体文本格式, 由插件自行处理, *猎影插件接口* 不做统一规定. 
  
  + **`last_update`** 类型: 字符串 *(单行文本)* <br />
    最后 更新时间. 比如 *3天前* 等. 
    
    具体文本格式, 由插件自行处理, *猎影插件接口* 不做统一规定. 
  
  + **`quality`** 类型: 字符串 *(单行文本)* <br />
    视频 质量 (清晰度, 分辨率) 的 描述信息. 
    比如 *4K*, *1080p*, *720p*, *低清*, 等. 
  
  + **`site`** 类型: 字符串 *(单行文本)* <br />
    视频所在 网站. 比如 *搜狐视频*, *乐视*, 等. 
  
  + **`size_byte`** 类型: 整数 <br />
    视频的 大小, 以 *字节* (`Byte`) 为 单位. 
  
  + **`size_px`** 类型: 数组 (`list`, `[]`, `Array`) <br />
    视频 大小 (分辨率), 以 *像素* (`px`) 为 单位. 
    
    数组 内为 2 个 整数, 分别表示 `x`, `y` 方向的 分辨率. 
    
    比如 `1080p` 视频的分辨率 可以 表示为 `[1920, 1080]`. 
  
  + **`subtitle`** 类型: 字符串 *(单行文本)* <br />
    视频小标题. 
    
    具体文本格式, 由插件自行处理, *猎影插件接口* 不做统一规定. 
  
  + **`time_s`** 类型: 整数 <br />
    视频 时长, 以 *秒* (`s`) 为 单位. 
  
  + **`year`** 类型: 字符串 *(单行文本)* <br />
    视频 年代 信息. 
    
    比如 *2015*, *90年代*, 等. 
    
    具体文本格式, 由插件自行处理, *猎影插件接口* 不做统一规定. 

+ **更多说明**
  
  + 参数 中的 `start` 是 期望的 开始条目 序号, 
    返回数据中的 `start` 是 实际返回的 开始条目 序号. 


## 3. 示例

测试 示例 请见 
**[猎影插件接口 示例](https://github.com/sceext2/lieying_plugin/blob/port_version-0.3/doc/lieying_plugin_test.md)**. 

**内容目录**

+ 0. 导入插件 其他初始化工作
+ 1. 测试 `GetVersion()`
+ 2. 测试 `Parse()`
  + 2.1 *多视频解析* 测试
  + 2.2 *单视频解析* 测试
+ 3. 测试 `ParseURL()`


:: end lieying_plugin.md, <https://github.com/sceext2/lieying_plugin>


